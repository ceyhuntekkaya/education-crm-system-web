export interface PanelOption {
  id: string;
  label: string;
  icon: string;
  href: string;
  description: string;
  color: string;
  bgColor: string;
}

export interface PanelMenuProps {
  // Props eklenebilir
}
