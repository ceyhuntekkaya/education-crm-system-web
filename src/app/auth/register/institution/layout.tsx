import React from "react";

/**
 * Institution Register Layout
 * Basit wrapper - provider'lar RegisterForm içinde
 */
const InstitutionRegisterLayout: React.FC<{ children: React.ReactNode }> = ({
  children,
}) => {
  return <>{children}</>;
};

export default InstitutionRegisterLayout;
