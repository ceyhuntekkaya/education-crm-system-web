import React from "react";

/**
 * User Register Layout
 * Veli kayıt layout'u
 */
const UserRegisterLayout: React.FC<{ children: React.ReactNode }> = ({
  children,
}) => {
  return <>{children}</>;
};

export default UserRegisterLayout;
