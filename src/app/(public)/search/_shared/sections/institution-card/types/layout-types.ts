// Layout component props - Context kullanımı ile büyük ölçüde azaltıldı

/**
 * Compact Layout Props - sadece callback props
 */
export interface CompactLayoutProps {
  onCardClick?: () => void;
}

/**
 * Expanded Layout Props - sadece callback props
 */
export interface ExpandedLayoutProps {
  onCardClick?: () => void;
}
