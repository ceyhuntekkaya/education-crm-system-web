export enum CampaignSchoolStatus {
  PENDING = "PENDING", // Beklemede
  ACTIVE = "ACTIVE", // Aktif
  PAUSED = "PAUSED", // Duraklatıldı
  REJECTED = "REJECTED", // Reddedildi
  EXPIRED = "EXPIRED", // Süresi doldu
  COMPLETED = "COMPLETED", // Tamamlandı
  REMOVED = "REMOVED", // Kaldırıldı
}
